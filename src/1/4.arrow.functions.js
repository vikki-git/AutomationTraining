let multiply = function (x, y) {
    return x * y;
};

const multiplyArrow = (x, y) => {
    return x * y
};

const multiplyArrowv2 = (x, y) => x * y;

console.log(multiply(2, 5));
console.log(multiplyArrow(2, 5));
console.log(multiplyArrowv2(2, 5));
